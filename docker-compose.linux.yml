services:
  app:
    # Linux-only override: share host network so localhost Ollama is reachable
    # even when Ollama listens on 127.0.0.1.
    network_mode: host
    ports: !reset []
    environment:
      OLLAMA_HOST: http://localhost:11434
